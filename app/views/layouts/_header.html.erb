<!-- <nav class="navbar navbar-inverse" >
  <div class="container">
    <%#= link_to "Home", root_path, :class => "navbar-brand" %>
    <%= link_to "Home", root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
         <% if user_signed_in? %>
          <li><%= link_to "Logout", destroy_user_session_path, :class=> "navbar-brand" %></li>
          <li><%= link_to "Profile", edit_user_registration_path, :class=>"navbar-brand" %></li>
         <% else %>
          <li><%= link_to "Sign In", new_user_session_path, :class=>"navbar-brand" %></li>
          <li><%= link_to "Sign Up", new_user_registration_path, :class=>"navbar-brand" %></li>
         <% end %>
      </ul>
    </nav>
  </div>
</nav>
    -->

<header class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="navbar-inner">
      
      <div class="container">
        <div class="navbar-header">
          <a class="navbar-brand" href="<%= root_path %>">Samosys</a>
        </div>
        <ul class="nav navbar-nav">
        <% if user_signed_in? %>
          <li><a href="<%= destroy_user_session_path %>">Logout</a></li>
          <li><a href="<%= edit_user_registration_path %>">Profile</a></li>
        <% else %>
          <li><a href="<%= new_user_session_path%>">Sign In</a></li>
          <li><a href="<%= new_user_registration_path%>">Sign Up</a></li>
        <% end %>
        </ul>
      </div>
    </div>
  </header>   